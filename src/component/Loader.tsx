import { Loader as LucideLoader } from "lucide-react";

const Loader = () => {
  return (
    <div>
      <LucideLoader size={15} className="animate-spin" />
    </div>
  );
};

export default Loader;
